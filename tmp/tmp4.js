const box = document.createElement('input');
box.type = 'checkbox';
box.checked = true;

export default box.outerHTML;

/**
 * 1) <input type=checkbox checked>
 * 2) <input checked>
 * 3) <input type=checkbox>
 */


////////////////////////////////////////////////////////

(({ a, header, main, footer, input, button, img, video, canvas, data }) => {

    // make magic...

})(window.htmlmodule);
